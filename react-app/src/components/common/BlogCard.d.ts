interface BlogCardProps {
    id: string;
    img: string;
    title: string;
    excerpt: string;
}
export declare const BlogCard: ({ id, img, title, excerpt }: BlogCardProps) => import("react/jsx-runtime").JSX.Element;
export {};
